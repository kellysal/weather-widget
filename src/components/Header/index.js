import React from "react";

const Header = () => (
    <div>
        <h1 className="header-container__title">Weather Widget</h1>
        <p className="header-container__subtitle">Enter city and country for current weather</p>
    </div>
);

export default Header;